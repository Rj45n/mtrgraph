apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mtrgraph

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - deployment-http-daemon.yaml
  - deployment-web.yaml
  - service-web.yaml
  # Décommente si tu veux aussi du MTR continu (CAP_NET_RAW requis) :
  # - deployment-mtr-daemon.yaml
  # Alternative au Deployment + http-daemon — CronJob ponctuel :
  # - cronjob-http.yaml

# Pour pointer vers ton registry privé :
# images:
#   - name: mtrgraph
#     newName: registry.example.com/infra/mtrgraph
#     newTag: "0.1.0"
